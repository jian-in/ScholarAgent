# 安全策略

ScholarAgent 会调用外部模型和论文数据源，也允许把研究笔记保存在本机。安全问题主要
集中在密钥管理、文件路径、外部响应和本地数据边界。

## 支持范围

当前维护版本为默认分支的最新提交。报告问题前，请先确认能够在最新版本复现。

## 报告安全漏洞

请优先使用仓库的 **GitHub Security Advisory** 私密报告入口，不要在公开 Issue 中
粘贴 API Key、个人研究资料、完整利用代码或尚未修复的漏洞细节。

如果私密报告入口不可用，可以先创建一个不含敏感细节的 Issue，请维护者提供私下
沟通方式。报告中建议包含受影响版本、最小复现步骤、影响范围和建议修复方向。

## 使用者安全边界

- 真实密钥只写入本机 `.env`；不要提交 `.env`，也不要把密钥写进截图或日志。
- `data/`、论文下载文件和评测运行产物默认不进入 Git。
- 只从可信来源安装依赖，并在升级依赖后重新运行完整测试。
- 公开运行记录前检查模型输入、输出、论文笔记和错误信息是否包含私人数据。
- Web 工作台默认用于本机访问；暴露到局域网或公网前需要自行增加认证、TLS 和访问控制。

## 不属于安全漏洞的情况

普通模型幻觉、论文结论质量和第三方 API 暂时不可用通常属于功能或质量问题，可按
普通 Issue 报告；如果它们能够导致越权、数据泄露或任意文件访问，则按安全漏洞处理。
