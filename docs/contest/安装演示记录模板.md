# 安装到演示现场记录模板

> 只在干净临时环境真实执行后填写。此文件是记录模板，不预填耗时、成功率或模型
> 结果；正式记录应附对应终端截图或日志路径，并删除任何 API Key。

| 项目 | 实际记录 |
|---|---|
| 日期/操作系统 |  |
| Python 版本 | 3.10 / 3.11 / 3.12：实际值 |
| 安装命令 | `python -m pip install .` 或实际命令 |
| wheel 文件名与 SHA-256 |  |
| 安装耗时 | 真实秒数 |
| `scholaragent --demo` 耗时 | 真实秒数 |
| `scholaragent-web --port …` 启动耗时 | 真实秒数 |
| 浏览器访问地址 | 仅记录本机回环地址 |
| 离线回放案例 | 案例目录与运行 ID |
| 失败/缺口 | 如实记录，不用“成功”替代 |

## 现场命令

```powershell
python -m venv .venv-clean
.venv-clean\Scripts\python.exe -m pip install .
.venv-clean\Scripts\scholaragent.exe --demo
.venv-clean\Scripts\scholaragent-web.exe --port 8766
```

如果环境没有网络或没有依赖缓存，记录安装失败原因；不要将安装失败改写为成功证据。
