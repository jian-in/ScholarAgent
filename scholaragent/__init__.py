"""ScholarAgent:面向科研文献调研的自主智能体系统(毕业设计)。

从零实现的 Agent 框架,不依赖 LangChain 等黑盒库,
每一层都能看清"大模型是怎么一步步变成智能体的"。

分层结构(自底向上):
    config.py  配置层   —— 统一读取 API Key 等配置
    llm.py     模型层   —— 唯一和大模型 API 打交道的地方
    tool.py    工具层   —— 给大模型装上"手"
    agent.py   智能体层 —— 思考→行动→观察 的核心循环
    tools/     内置工具 —— 计算器、时钟等具体工具实现
"""

__version__ = "0.1.0"
